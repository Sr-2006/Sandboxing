# ==============================================================================
# Federation URLs — LIVE tunnel URLs. Share this file with the team (commit it).
# Quick-tunnel URLs change every time start-tunnel.ps1 restarts — update then.
# ==============================================================================

# Laptop A — Phase 1 telemetry/dataset endpoint (serve_dataset.py)
PHASE1_DATASET_URL=https://tokyo-equivalent-reduce-integrating.trycloudflare.com

# Laptop B — Phase 2 ChromaDB (chroma run --port 8000)
PHASE2_CHROMA_URL=

# Laptop C — Phase 3 FastMCP SSE servers + Ollama
PHASE3_MCP_URL=
PHASE3_OLLAMA_URL=

# Laptop D — Phase 4 veto/security gateway
PHASE4_VETO_URL=
